<!DOCTYPE html>
<html lang="en">
<head>
    <title>Messenger</title>
    <link href="/css/site.css" type="text/css" rel="stylesheet" />
</head>
<body>
<div class="container">
    <ul class="menu">
        <li><a href="/">Home</a></li>
        <li>&nbsp;&nbsp;&nbsp;</li>
        {{#if currentUser}}
            <li>
                <form id="logout-form" action="/users/logout" method="POST"></form>
                <a href="javascript:document.getElementById('logout-form').submit()">Logout</a>
            </li>
            <li>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;</li>
            <li class='avatar-holder'>Welcome {{currentUser.username}} <img src='{{currentUser.avatar}}' class='avatar-user-menu' /></li>
        {{else}}
            <li><a href="/users/register">Register</a></li>
            <li><a href="/users/login">Login</a></li>
        {{/if}}
    </ul>

    {{#if currentUser}}
        <form action="/thread/" method="get">
            <input type="text" name="username">
            <input type="submit" value="Search User To Message with">
        </form>
    {{/if}}

    {{#if globalError}}
        <h3>{{globalError}}</h3>
    {{/if}}
    {{{body}}}
</div>
</body>
</html>